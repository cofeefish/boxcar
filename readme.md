# INSTALLATION
from the .../boxcar directory
1. python -m venv .venv
2. .venv\Scripts\Activate.ps1
3. python -m pip install -r boxcar/requirements.txt

to run:
1. run the boxcar.bat file in the scripts dir
2. open the link in the new terminal